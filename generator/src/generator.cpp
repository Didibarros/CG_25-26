#include <iostream>
#include <cstring>
#include "utils.hpp"
#include "model.hpp"

using namespace std;

const char* instructions = "Usage:\n - 'plane {length} {divisions} {filename}'\n - 'box {length} {divisions} {filename}'\n - 'sphere {radius} {slices} {stacks} {filename}'\n - 'cone {radius} {height} {filename}'\n";

int main(const int argc, char** argv)
{
	return 0;
}

